# Add custom ProGuard rules here if you enable minification.
# This file exists so Gradle release builds can reference it without failing.
# Keep it empty unless you need to preserve classes or methods during shrinking.
